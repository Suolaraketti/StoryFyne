# Product screenshots (real UI assets)

Drop the real Dialfyne portal screenshots here with these exact filenames, commit,
and the builder will frame them (browser chrome + shadow on the dark stage) and
animate them with camera moves + highlight callouts. PNG preferred (transparent or
not — they get framed either way). Higher-res = better for push-ins.

| filename                      | screenshot                          | used in                                  |
|-------------------------------|-------------------------------------|------------------------------------------|
| connected-apps.png            | Connected apps / integrations list  | Integrations scene (CRM/Gong/HubSpot)    |
| metrics-row.png               | 4 KPI cards (calls/appts/score)     | Outcome scene (reps get better, faster)  |
| revenue-command-center.png    | Full Revenue Command Center         | Hero "this is the product" reveal        |
| email-assist.png              | Lead follow-up / Write with Dialfyne| Feature beat (AI writes the follow-up)   |
| command-center-card.png       | "Open Command Center" card          | Transition / CTA accent                  |

Notes:
- Keep them as exported from the portal (light theme is fine — they're framed in dark chrome).
- If you have a 2x / retina export, use that; camera push-ins crop in, so resolution helps.
- Add more anytime; tell me the filename + which beat and I'll wire it.
